# ── Supabase ──────────────────────────────────────────────────────────────
# Project URL — encontrarlo en Project Settings → API → Project URL
VITE_SUPABASE_URL=https://ditmbqkvzreekqnkimqv.supabase.co

# Anon (publishable) key — encontrarla en Project Settings → API → Project API keys
# Esta key es PÚBLICA — va al cliente. RLS la limita a operaciones permitidas.
# NO usar el service_role key acá nunca.
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs__REPLACE_WITH_REAL_ANON_KEY__
