{
  "name": "macario-lite",
  "private": true,
  "version": "1.0.0",
  "description": "Sistema de gestión interno Justo Makario — desktop (web/) + mobile PWA (mobile/) servidos como estáticos. Backend Supabase (auth + RPCs + Edge Functions).",
  "scripts": {
    "dev:web": "echo 'web/ es estático — abrí web/Macario Lite.html con un static server (npx serve web)'",
    "dev:mobile": "echo 'mobile/ es estático — npx serve mobile, abrí http://localhost:3000/'",
    "build": "echo 'No requiere build — todo es estático.'",
    "test": "node tests/run-all.js"
  },
  "devDependencies": {
    "@babel/standalone": "7.29.0",
    "jsdom": "^29.1.1",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
